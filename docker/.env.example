# =============================================================================
# Variables de Entorno - Docker Swarm Stack
# Copia este archivo como .env y completa los valores
# =============================================================================

# --- PostgreSQL ---
POSTGRES_PASSWORD=tu-password-seguro-aqui

# --- JWT (genera uno con: openssl rand -base64 32) ---
JWT_SECRET=tu-jwt-secret-de-al-menos-32-caracteres

# --- Supabase Keys (genera con: https://supabase.com/docs/guides/self-hosting#api-keys) ---
SUPABASE_ANON_KEY=eyJ...tu-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJ...tu-service-role-key
SUPABASE_PROJECT_ID=self-hosted

# --- URLs ---
SUPABASE_PUBLIC_URL=http://tu-servidor:8000
SITE_URL=http://tu-servidor:3000
ADDITIONAL_REDIRECT_URLS=

# --- SMTP (para emails de auth) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password
SMTP_ADMIN_EMAIL=noreply@tudominio.com

# --- API Keys (de tu app actual) ---
LOVABLE_API_KEY=
DEEPSEEK_API_KEY=
GOOGLE_CLOUD_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
GLOBAL_S3_ENDPOINT=
